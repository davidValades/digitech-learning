<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.SmartOrder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores principales -->
        <item name="android:statusBarColor">@color/brand_purple</item>        <item name="colorSecondary">@color/brand_purple</item>
        <item name="colorTertiary">@color/brand_teal</item>

        <!-- Colores de fondo y superficies -->
        <item name="android:windowBackground">@color/bg_light</item>
        <item name="colorOnBackground">@color/text_dark</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.SmartOrder" parent="Base.Theme.SmartOrder" />
</resources>